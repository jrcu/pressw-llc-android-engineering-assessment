<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plain HTTP only to the local backend hosts used during development
  (10.0.2.2 is the emulator's alias for the host machine's localhost; adb
  reverse forwards target localhost directly). Everything else still
  requires HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
